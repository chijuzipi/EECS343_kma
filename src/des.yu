
NOTE: it defined a minimum buffer size

typedef struct bufferT
{
    kma_size_t size;
    void* ptr;
    struct bufferT* next_size;
    struct bufferT* next_buffer;
    struct bufferT* prev_buffer;
    kpage_t* page; //point to the page struct the buffer reside
} buffer_t;



1) void init_free_list(void):
    
